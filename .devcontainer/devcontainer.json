{
	"name": "Go",
	
	// Dockerfile のパスを指定します
	"build": {
	  "dockerfile": "Dockerfile",
	  "context": ".."
	},

	"customizations": {
	  "vscode": {
		"extensions": [
		  "jebbs.plantuml"
		],
		"settings": {
		  "plantuml.server": "http://localhost:8080"
		}
	  }
	},

	"forwardPorts": [
	  8080
	],

	"remoteUser": "root"
  }
